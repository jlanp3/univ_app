  <!-- Dropdown Structure -->
  <ul id="dropdown1" class="dropdown-content">
    <% if logged_in? and current_user.admin? %>
      <li><%= link_to "Admin", student_path(current_user) %></li>
      <li class="divider"></li>
    <% elsif logged_in? %>
      <li><%= link_to "#{current_user.name}", student_path(current_user) %></li>
      <li class="divider"></li>
    <% end %>
      <li><%= session_link %></li>
    <% if !logged_in? %>
      <li><%= link_to "Sign up", signup_path %></li>
    <% end %>
  </ul>
  <ul id="dropdown2" class="dropdown-content">
    <% if logged_in? and current_user.admin? %>
      <li><%= link_to "Admin", student_path(current_user) %></li>
      <li class="divider"></li>
    <% elsif logged_in? %>
      <li><%= link_to "#{current_user.name}", student_path(current_user) %></li>
      <li class="divider"></li>
    <% end %>
      <li><%= session_link %></li>
    <% if !logged_in? %>
      <li><%= link_to "Sign up", signup_path %></li>
    <% end %>
  </ul>
  <nav class="bg-secondary">
    <div class="container">
      <div class="nav-wrapper">
        <%= link_to image_tag("canvas.png", alt: "University App", size: "256x32", class: "responsive-img"), root_path, class: "brand-logo", id: "logo" %>
        <a href="#" data-target="mobile-demo" class="sidenav-trigger"><i class="material-icons">menu</i></a>
        <ul class="right hide-on-med-and-down">
          <li><%= link_to "Courses", root_path %></li>
          <% if logged_in? %>
            <li><%= link_to "Estudents", students_path %></li>
          <% end %>
          <!-- Dropdown Trigger -->
          <li><a class="dropdown-trigger" href="#!" data-target="dropdown1">Account<i class="material-icons right">arrow_drop_down</i></a></li>
        </ul>
      </div>
    </div>
  </nav>

  <ul class="sidenav" id="mobile-demo">
    <div class="center">
      <%= link_to image_tag("canvas.png", alt: "University App", size: "128x16", class: "responsive-img"), root_path, class: "brand-logo", id: "logo" %>
    </div>
    <li><%= link_to "Courses", root_path %></li>
    <% if logged_in? %>
      <li><%= link_to "Estudents", students_path %></li>
    <% end %>
    <li><a class="dropdown-trigger" href="#!" data-target="dropdown2">Account<i class="material-icons right">arrow_drop_down</i></a></li>
  </ul>